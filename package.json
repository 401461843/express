{
  "name": "express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "pm2 deploy ecosystem.config.js production setup",
    "fetch": " pm2 deploy ecosystem.config.js production",
    "reboot": "pm2 deploy production exec 'pm2 startup'",
    "save": "pm2 deploy production exec 'pm2 save'",
    "update": "pm2 deploy production update",
    "stop": "pm2 deploy production exec 'pm2 stop all'",
    "restart": "cross-env NODE_ENV=production pm2 deploy production exec 'pm2 restart all'",
    "dev": "cross-env NODE_ENV=dev  nodemon ./bin/www",
    "lint": "eslint .",
    "fix": "eslint --fix ."
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "connect-redis": "^4.0.4",
    "cookie-parser": "~1.4.4",
    "cross-env": "^7.0.2",
    "crypto": "^1.0.1",
    "debug": "~2.6.9",
    "ejs": "^3.1.3",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "moment": "^2.26.0",
    "morgan": "~1.9.1",
    "mysql": "^2.18.1",
    "redis": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^7.4.0",
    "husky": "^4.2.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo 'husky' && npm run lint"
    }
  }
}
