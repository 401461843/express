{
  "name": "express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup":"pm2 deploy ecosystem.config.js production setup",
    "fetch":"pm2 deploy ecosystem.config.js production",
    "update":"pm2 deploy production update",
    "start": "pm2 start ./bin/www",
    "prd":"pm2 start ecosystem.config.js --env production",
    "restart": "pm2 deploy production exec 'pm2 startup'",
    "restart": "pm2 deploy production exec 'pm2 save'",
    "prdreload":"pm2 deploy production exec 'pm2 reload all'"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1"
  }
}
